# Shell scripts must keep LF endings: a CRLF shebang (`#!/bin/sh\r`) is not
# executable inside the Linux container.
*.sh text eol=lf

# Keep everything the image or the interpreter reads consistent too.
Dockerfile text eol=lf
*.py text eol=lf
*.json text eol=lf
